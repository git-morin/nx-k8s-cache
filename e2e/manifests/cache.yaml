apiVersion: apps/v1
kind: Deployment
metadata:
  name: cache
  namespace: e2e
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cache
  template:
    metadata:
      labels:
        app: cache
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: cache
          image: nx-cache-server:ci
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
            - name: NX_CACHE_TOKEN
              valueFrom:
                secretKeyRef:
                  name: cache-secret
                  key: token
            - name: NX_CACHE_DIR
              value: /cache
          volumeMounts:
            - name: cache-storage
              mountPath: /cache
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 200m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8080
            initialDelaySeconds: 1
            periodSeconds: 2
            failureThreshold: 3
      volumes:
        - name: cache-storage
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: cache
  namespace: e2e
spec:
  selector:
    app: cache
  ports:
    - port: 8080
      targetPort: 8080
